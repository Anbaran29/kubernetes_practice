apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: db-dafienz
spec:
  selector:
    app: adminer
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    nodePort: 30013
  type: NodePort
